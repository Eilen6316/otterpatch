/**
 * 与具体格式无关的"对话/多步循环"提示词:意图路由前导、催收(thought→action)、各兜底文案、
 * 以及通用工具(answer_user / read_range / aggregate)的描述。改这些只动这里。
 */

/** 路由前导:让模型自己判断『回答问题』还是『提出改动』,并果断行动。 */
export const ROUTING_PREAMBLE =
  '判断用户意图后【果断行动】:① 若用户是在提问/查询/咨询(如"这列平均值多少""哪几行可能有问题""这个公式什么意思"),用 answer_user 工具给出简洁文字回答,【绝不要】修改表格;' +
  '② 凡是明确的执行/生成类指令(如"补全公式""标红异常值""统一格式""mock/造 N 行数据""把这列改成…""画一个 X 图"),【直接调用修改工具执行】——采用合理默认,把关键假设在 plan 里一句话讲清即可,【不要因为可以更完美而反复反问、也不要只输出分析而不动手】;' +
  '③ 只有当缺失信息会直接导致明显错误、且你无法合理假设时,才用 answer_user 澄清,且最多问一次、并给出你的推荐默认;' +
  '④ 上下文只给了大表样本时,需要更精确数据就调 read_range/aggregate 工具按需取,不要凭样本臆测;' +
  '⑤ 不要把思考写成长篇而不产出工具调用——想清楚后必须落到一次 propose_changeset(改表)或 answer_user(回答)。';

/** 模型只思考、没产出工具调用/正文时,催它直接给结果(最多催一次)。 */
export const NUDGE_DIRECT =
  '请基于刚才的思考【现在直接给出结果】:要改表/画图就调用 propose_changeset 工具,否则用 answer_user 回答;不要再只输出思考过程。';

/** 催收后仍无结果时返回给用户的话。 */
export const EMPTY_RESULT_FALLBACK =
  '我已分析但这次没产出可用结果。请重试,或把需求说得更具体;若是大量/复杂改动,建议分批,或换用支持更长输出的模型。';

/** 工具入参被输出长度截断、连兜底解析都救不回时返回给用户的话。 */
export const TRUNCATED_FALLBACK =
  '这次要改的内容太多,超出模型单次输出长度被截断了。请缩小范围或分批(例如先处理前 20 行/前几个节点),或换用支持更长输出的模型。';

/** 多步 loop 超出步数上限时的兜底话。 */
export const TOO_MANY_STEPS_MSG = '处理步数过多,请缩小问题范围或把指令说得更具体。';

// 通用工具描述(answer_user / read_range / aggregate)
export const ANSWER_USER_DESC = '当用户是在提问/查询/咨询表格(而不是要修改它)时,用本工具直接给出文字回答或澄清反问。';
export const READ_RANGE_DESC = '读取整张表里任意 A1 区域的精确单元格值(用于超出已给样本的数据)。';
export const AGGREGATE_DESC = '对某一整列做聚合统计(自动跳过表头);可选 groupBy 按某列分组(做透视/分组汇总,如"各产品销量合计"),可选 where 先按条件筛选行再聚合。';
