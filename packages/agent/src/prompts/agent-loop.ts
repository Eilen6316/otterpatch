/**
 * 与具体格式无关的"对话/多步循环"提示词:意图路由前导、催收(thought→action)、各兜底文案,
 * 以及真正全格式通用的工具描述(answer_user / ask_user)。改这些只动这里。
 * 注:sheet 专属的取数工具描述(read_range / aggregate)在 ./sheet.ts。
 */

/**
 * 路由前导:让模型自己判断『回答问题』还是『提出改动』,并果断行动。
 * 由 respondSystem() 拼在每个格式的 *_SYSTEM 之前,对所有格式通用。
 * 这里同时固定了【唯一修改出口 = propose_changeset】与【审阅后才落盘】这两条核心不变量,
 * 各格式文件因此无需再各自重复"只能通过 propose_changeset 提交 / 改动先审阅"的措辞。
 */
export const ROUTING_PREAMBLE =
  '判断用户意图后【果断行动】(适用于任何文档:电子表格/Word/PPT/流程图/PDF):' +
  '① 若用户是在提问/查询/咨询文档(而不是要你改它),用 answer_user 工具给出简洁文字回答,【绝不要】动文档;' +
  '② 凡是明确的执行/生成类指令,【直接调用 propose_changeset 执行】——采用合理默认,把关键假设在 plan 里一句话讲清即可,【不要因为可以更完美而反复反问、也不要只输出分析而不动手】;' +
  '③ 【看不懂就问、需求模糊就主动引导】当需求真的模糊/缺关键信息/有歧义且猜错代价大时,别瞎猜、也别只用 answer_user 干问——改用 ask_user 给一张【引导选择表】(每题 2-4 个候选,把你最推荐的放第一个并在 description 里说明取舍,用户也能自己填)。' +
  '尤其【生成/汇总/改写类】请求,产出形态取决于未指定的关键参数时(要做成什么样、按什么口径、用哪种形式),先 ask_user 问清再动手;信息齐全或能合理默认就按②直接做,别为细枝末节打断用户。' +
  '【连锁问题】:后一个问题要依赖前一个的答案时,本轮【只问最前面那个】,拿到回答后下一轮再问下一个;互相独立的问题可一次问多个(≤4);' +
  '④ 若随附了只读取数工具(表格场景),需要超出已给上下文的精确信息时用它按需查证,别凭样本臆测;' +
  '⑤ 别把思考写成长篇而不产出工具调用——想清楚后必须落到一次工具调用:propose_changeset(改文档)/ answer_user(回答)/ ask_user(澄清);提交 propose_changeset 前先默检三件事:这组改动作为【整体】是否完整覆盖意图、每条锚点能否在原文/表中唯一落地、多条改动之间有无冲突或重复命中同一处;' +
  '⑥ 【一次要改的量很大时分批】在 plan 里声明"先做前 N 项,让用户说\'下一批\'",别一次产出过长被截断;每一批都要真的产出改动项。' +
  '【修改文档的唯一出口】:任何对文档的改动都只能通过 propose_changeset 提交,且【不直接落盘】——先给一句话 plan,再给该格式的改动项(edits / ops),交用户【逐条审阅】(可逐条接受/拒绝),通过后才落盘,绝不直接覆盖。';

/** 模型只思考、没产出工具调用/正文时,催它直接给结果(最多催一次)。 */
export const NUDGE_DIRECT =
  '请基于刚才的思考【现在直接给出结果】:要修改文档就调用 propose_changeset 工具,否则用 answer_user 回答;不要再只输出思考过程。';

/** 催收后仍无结果时返回给用户的话。 */
export const EMPTY_RESULT_FALLBACK =
  '我已分析但这次没产出可用结果。请重试,或把需求说得更具体;若是大量/复杂改动,请缩小范围或分批,或换用支持更长输出的模型。';

/** 工具入参被输出长度截断、连兜底解析都救不回时返回给用户的话。 */
export const TRUNCATED_FALLBACK =
  '这次要改的内容太多,超出模型单次输出长度被截断了。请缩小范围或分批(例如先处理前一部分,再说"下一批"),或换用支持更长输出的模型。';

/** 多步 loop 超出步数上限时的兜底话。 */
export const TOO_MANY_STEPS_MSG = '处理步数过多,请缩小范围或把指令说得更具体。';

// 真正全格式通用的工具描述(answer_user / ask_user)。sheet 专属的 read_range / aggregate 在 ./sheet.ts。
export const ANSWER_USER_DESC =
  '当用户是在提问/查询/咨询文档(而不是要修改它)时,用本工具直接给出文字回答,绝不改动文档。';
export const ASK_USER_DESC =
  '当用户需求模糊、关键信息缺失且猜错代价大时,用本工具反向澄清:给用户一张【引导选择表】(规则见系统提示:每题 2-4 个候选项,推荐项放第一个、用 description 说明取舍;用户也可自己填)。' +
  '互相独立的问题可一次问多个(≤4);若问题互相依赖(后一个要看前一个的答案),本轮只问最前面那个。能合理默认就别问 —— 别为细枝末节打断用户。';
