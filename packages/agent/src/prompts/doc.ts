/**
 * Word 文档专属的【只读取数工具】描述。
 * 上下文里每段正文有截断(超长段只给开头),这四条让模型能像专家一样"先通读再动手":
 * 按段取全文、全文检索、要文档大纲、看样式使用分布(排版审计的原料)。
 * 仅在有文档快照(hasDoc)时由 sheet-tools.ts 的 auxToolDefs 挂载。
 */

export const READ_BLOCKS_DESC =
  '按段号区间读取文档段落的【完整原文】(上下文里超长段落已被截断;要引用/改写某段前,先用本工具拿到全文,quote 必须来自真实原文)。';

export const FIND_TEXT_DESC =
  '在全文中检索一个片段/关键词,返回每处命中的段号与上下文摘录(用于确认 quote 唯一性、找同类问题的所有出现位置)。';

export const GET_OUTLINE_DESC =
  '返回文档大纲(标题层级树 + 各级标题文字与段号),用于诊断结构:层级是否越级、标题是否规范、信息组织是否合理。';

export const GET_STYLE_USAGE_DESC =
  '返回全文样式使用分布(每种 字体×字号×对齐×行距 组合的段数与示例段号),用于排版审计:字体字号是否忽大忽小、正文基线是否统一、有无手动大字冒充标题。';
