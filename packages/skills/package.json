{
  "name": "@otterpatch/skills",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate:playbooks": "node scripts/generate-playbooks.mjs",
    "prebuild": "npm run generate:playbooks",
    "build": "tsc -b",
    "pretest": "npm run generate:playbooks",
    "test": "node --import tsx --test src/skills.test.ts src/playbooks.test.ts"
  },
  "dependencies": {
    "@noble/hashes": "2.2.0"
  },
  "devDependencies": {
    "tsx": "^4.23.8"
  }
}
